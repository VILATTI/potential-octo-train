machine:
  ruby:
    version:
      2.3.0
  post:
    - ./daemon:
          background: true
test:
  override:
    - bundle exec rubocop:
        parallel: true
    - bundle exec rspec:
        parallel: true
    - bundle exec cucumber:
        parallel: true
deployment:
  staging:
    branch: master
    heroku:
      appname: limitless-spire-56966
